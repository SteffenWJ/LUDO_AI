# LUDO_AI


This is a genetic algorithm that tries to train a neural network's weights. This is not the standard way it is done, but I thought it would be fun to see if a genetic algorithm could train a convolutional network to control a LUDO piece.

The Answer is yes, but not effectively, as there is too little information and options to make it viable as a whole.
